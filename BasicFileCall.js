const sum = (num1, num2) => num1 + num2;
module.exports = sum

/*In another file eg app.js write this code to access sum Method

//assuming file is in same path
const BasicFileCall = require('./BasicFileCall.js');

console.log(tutorial(1,1));*/
